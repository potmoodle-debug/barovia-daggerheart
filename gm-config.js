// Public configuration only. Never put secret/service-role keys here.
// When the private GM backend is provisioned, set this to its read-only
// player snapshot endpoint. Leaving it blank keeps the site on static data.
window.BAROVIA_PUBLIC_FEED_URL = "";
